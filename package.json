{
  "name": "e-shop-server",
  "version": "1.0.0",
  "description": "JSON Server for E-Shop",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 5001",
    "dev": "json-server --watch db.json --port 5001"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
